{
  "name": "openlayers-largecluster-demo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace @demo/server",
    "dev:app": "npm run dev --workspace @demo/app",
    "build": "npm run build --workspace @demo/server && npm run build --workspace @demo/app",
    "typecheck": "npm run typecheck --workspace @demo/server && npm run typecheck --workspace @demo/app"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

